import { NewGameObjectEvent } from './new-game-object-event';

describe('NewGameObjectEvent', () => {
  it('should create an instance', () => {
    expect(new NewGameObjectEvent()).toBeTruthy();
  });
});
