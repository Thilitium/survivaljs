export enum CreepType {
	Unknown,
	Archer,
	Basher
}

export enum Position {
	Mid,
	Left,
	TopLeft,
	Top,
	TopRight,
	Right,
	BottomRight,
	Bottom,
	BottomLeft
}

export enum UiLayer {
	BACKGROUND,
	GROUND,
	SELECTIONS,
	BUILDINGS,
	CREEPS,
	FX,
	INFO,
	UI
}
